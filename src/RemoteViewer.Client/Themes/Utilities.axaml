<Styles xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mi="using:Material.Icons.Avalonia">

    <!-- ============================================== -->
    <!-- TAILWIND-LIKE UTILITY CLASSES                  -->
    <!-- ============================================== -->
    <!-- Usage: <Border Classes="p-md m-sm"/>           -->
    <!-- Composable with component classes:             -->
    <!-- <Border Classes="card p-lg"/>                  -->

    <!-- ============================================== -->
    <!-- MARGIN UTILITIES (applies to any Control)      -->
    <!-- ============================================== -->

    <!-- Margin - All sides -->
    <Style Selector="Control.m-0">
        <Setter Property="Margin" Value="0"/>
    </Style>
    <Style Selector="Control.m-xxs">
        <Setter Property="Margin" Value="2"/>
    </Style>
    <Style Selector="Control.m-xs">
        <Setter Property="Margin" Value="4"/>
    </Style>
    <Style Selector="Control.m-sm">
        <Setter Property="Margin" Value="8"/>
    </Style>
    <Style Selector="Control.m-md">
        <Setter Property="Margin" Value="12"/>
    </Style>
    <Style Selector="Control.m-lg">
        <Setter Property="Margin" Value="16"/>
    </Style>
    <Style Selector="Control.m-xl">
        <Setter Property="Margin" Value="24"/>
    </Style>
    <Style Selector="Control.m-2xl">
        <Setter Property="Margin" Value="32"/>
    </Style>

    <!-- Margin - Horizontal (left + right) -->
    <Style Selector="Control.mx-0">
        <Setter Property="Margin" Value="0,0"/>
    </Style>
    <Style Selector="Control.mx-xxs">
        <Setter Property="Margin" Value="2,0"/>
    </Style>
    <Style Selector="Control.mx-xs">
        <Setter Property="Margin" Value="4,0"/>
    </Style>
    <Style Selector="Control.mx-sm">
        <Setter Property="Margin" Value="8,0"/>
    </Style>
    <Style Selector="Control.mx-md">
        <Setter Property="Margin" Value="12,0"/>
    </Style>
    <Style Selector="Control.mx-lg">
        <Setter Property="Margin" Value="16,0"/>
    </Style>

    <!-- Margin - Vertical (top + bottom) -->
    <Style Selector="Control.my-0">
        <Setter Property="Margin" Value="0,0"/>
    </Style>
    <Style Selector="Control.my-xxs">
        <Setter Property="Margin" Value="0,2"/>
    </Style>
    <Style Selector="Control.my-xs">
        <Setter Property="Margin" Value="0,4"/>
    </Style>
    <Style Selector="Control.my-sm">
        <Setter Property="Margin" Value="0,8"/>
    </Style>
    <Style Selector="Control.my-md">
        <Setter Property="Margin" Value="0,12"/>
    </Style>
    <Style Selector="Control.my-lg">
        <Setter Property="Margin" Value="0,16"/>
    </Style>

    <!-- Margin - Individual sides (Control base) -->
    <Style Selector="Control.mt-xxs">
        <Setter Property="Margin" Value="0,2,0,0"/>
    </Style>
    <Style Selector="Control.mt-xs">
        <Setter Property="Margin" Value="0,4,0,0"/>
    </Style>
    <Style Selector="Control.mt-sm">
        <Setter Property="Margin" Value="0,8,0,0"/>
    </Style>
    <Style Selector="Control.mt-md">
        <Setter Property="Margin" Value="0,12,0,0"/>
    </Style>
    <Style Selector="Control.mt-lg">
        <Setter Property="Margin" Value="0,16,0,0"/>
    </Style>

    <Style Selector="Control.mb-xxs">
        <Setter Property="Margin" Value="0,0,0,2"/>
    </Style>
    <Style Selector="Control.mb-xs">
        <Setter Property="Margin" Value="0,0,0,4"/>
    </Style>
    <Style Selector="Control.mb-sm">
        <Setter Property="Margin" Value="0,0,0,8"/>
    </Style>
    <Style Selector="Control.mb-md">
        <Setter Property="Margin" Value="0,0,0,12"/>
    </Style>
    <Style Selector="Control.mb-lg">
        <Setter Property="Margin" Value="0,0,0,16"/>
    </Style>
    <Style Selector="Control.mb-xl">
        <Setter Property="Margin" Value="0,0,0,24"/>
    </Style>

    <Style Selector="Control.ml-xxs">
        <Setter Property="Margin" Value="2,0,0,0"/>
    </Style>
    <Style Selector="Control.ml-xs">
        <Setter Property="Margin" Value="4,0,0,0"/>
    </Style>
    <Style Selector="Control.ml-sm">
        <Setter Property="Margin" Value="8,0,0,0"/>
    </Style>
    <Style Selector="Control.ml-md">
        <Setter Property="Margin" Value="12,0,0,0"/>
    </Style>

    <Style Selector="Control.mr-xxs">
        <Setter Property="Margin" Value="0,0,2,0"/>
    </Style>
    <Style Selector="Control.mr-xs">
        <Setter Property="Margin" Value="0,0,4,0"/>
    </Style>
    <Style Selector="Control.mr-sm">
        <Setter Property="Margin" Value="0,0,8,0"/>
    </Style>
    <Style Selector="Control.mr-md">
        <Setter Property="Margin" Value="0,0,12,0"/>
    </Style>

    <!-- Margin - Individual sides (Border override for specificity) -->
    <Style Selector="Border.mt-sm">
        <Setter Property="Margin" Value="0,8,0,0"/>
    </Style>
    <Style Selector="Border.mt-md">
        <Setter Property="Margin" Value="0,12,0,0"/>
    </Style>
    <Style Selector="Border.mt-lg">
        <Setter Property="Margin" Value="0,16,0,0"/>
    </Style>
    <Style Selector="Border.mb-sm">
        <Setter Property="Margin" Value="0,0,0,8"/>
    </Style>
    <Style Selector="Border.mb-md">
        <Setter Property="Margin" Value="0,0,0,12"/>
    </Style>
    <Style Selector="Border.mb-lg">
        <Setter Property="Margin" Value="0,0,0,16"/>
    </Style>
    <Style Selector="Border.mb-xl">
        <Setter Property="Margin" Value="0,0,0,24"/>
    </Style>
    <Style Selector="Border.ml-sm">
        <Setter Property="Margin" Value="8,0,0,0"/>
    </Style>
    <Style Selector="Border.ml-md">
        <Setter Property="Margin" Value="12,0,0,0"/>
    </Style>
    <Style Selector="Border.mr-sm">
        <Setter Property="Margin" Value="0,0,8,0"/>
    </Style>
    <Style Selector="Border.mr-md">
        <Setter Property="Margin" Value="0,0,12,0"/>
    </Style>

    <!-- ============================================== -->
    <!-- PADDING UTILITIES                              -->
    <!-- ============================================== -->
    <!-- Note: Padding is on Border (Decorator) and    -->
    <!-- TemplatedControl (Button, etc.) - need both   -->

    <!-- Padding - All sides - Border -->
    <Style Selector="Border.p-0">
        <Setter Property="Padding" Value="0"/>
    </Style>
    <Style Selector="Border.p-xxs">
        <Setter Property="Padding" Value="2"/>
    </Style>
    <Style Selector="Border.p-xs">
        <Setter Property="Padding" Value="4"/>
    </Style>
    <Style Selector="Border.p-sm">
        <Setter Property="Padding" Value="8"/>
    </Style>
    <Style Selector="Border.p-md">
        <Setter Property="Padding" Value="12"/>
    </Style>
    <Style Selector="Border.p-lg">
        <Setter Property="Padding" Value="16"/>
    </Style>
    <Style Selector="Border.p-xl">
        <Setter Property="Padding" Value="24"/>
    </Style>
    <Style Selector="Border.p-2xl">
        <Setter Property="Padding" Value="32"/>
    </Style>

    <!-- Padding - All sides - TemplatedControl (Button, etc.) -->
    <Style Selector="TemplatedControl.p-0">
        <Setter Property="Padding" Value="0"/>
    </Style>
    <Style Selector="TemplatedControl.p-xxs">
        <Setter Property="Padding" Value="2"/>
    </Style>
    <Style Selector="TemplatedControl.p-xs">
        <Setter Property="Padding" Value="4"/>
    </Style>
    <Style Selector="TemplatedControl.p-sm">
        <Setter Property="Padding" Value="8"/>
    </Style>
    <Style Selector="TemplatedControl.p-md">
        <Setter Property="Padding" Value="12"/>
    </Style>
    <Style Selector="TemplatedControl.p-lg">
        <Setter Property="Padding" Value="16"/>
    </Style>
    <Style Selector="TemplatedControl.p-xl">
        <Setter Property="Padding" Value="24"/>
    </Style>
    <Style Selector="TemplatedControl.p-2xl">
        <Setter Property="Padding" Value="32"/>
    </Style>

    <!-- Padding - Horizontal - Border -->
    <Style Selector="Border.px-xs">
        <Setter Property="Padding" Value="4,0"/>
    </Style>
    <Style Selector="Border.px-sm">
        <Setter Property="Padding" Value="8,0"/>
    </Style>
    <Style Selector="Border.px-md">
        <Setter Property="Padding" Value="12,0"/>
    </Style>
    <Style Selector="Border.px-lg">
        <Setter Property="Padding" Value="16,0"/>
    </Style>

    <!-- Padding - Horizontal - TemplatedControl -->
    <Style Selector="TemplatedControl.px-xs">
        <Setter Property="Padding" Value="4,0"/>
    </Style>
    <Style Selector="TemplatedControl.px-sm">
        <Setter Property="Padding" Value="8,0"/>
    </Style>
    <Style Selector="TemplatedControl.px-md">
        <Setter Property="Padding" Value="12,0"/>
    </Style>
    <Style Selector="TemplatedControl.px-lg">
        <Setter Property="Padding" Value="16,0"/>
    </Style>

    <!-- Padding - Vertical - Border -->
    <Style Selector="Border.py-xs">
        <Setter Property="Padding" Value="0,4"/>
    </Style>
    <Style Selector="Border.py-sm">
        <Setter Property="Padding" Value="0,8"/>
    </Style>
    <Style Selector="Border.py-md">
        <Setter Property="Padding" Value="0,12"/>
    </Style>
    <Style Selector="Border.py-lg">
        <Setter Property="Padding" Value="0,16"/>
    </Style>

    <!-- Padding - Vertical - TemplatedControl -->
    <Style Selector="TemplatedControl.py-xs">
        <Setter Property="Padding" Value="0,4"/>
    </Style>
    <Style Selector="TemplatedControl.py-sm">
        <Setter Property="Padding" Value="0,8"/>
    </Style>
    <Style Selector="TemplatedControl.py-md">
        <Setter Property="Padding" Value="0,12"/>
    </Style>
    <Style Selector="TemplatedControl.py-lg">
        <Setter Property="Padding" Value="0,16"/>
    </Style>

    <!-- Padding - Individual sides - Border -->
    <Style Selector="Border.pt-xs">
        <Setter Property="Padding" Value="0,4,0,0"/>
    </Style>
    <Style Selector="Border.pt-sm">
        <Setter Property="Padding" Value="0,8,0,0"/>
    </Style>
    <Style Selector="Border.pt-md">
        <Setter Property="Padding" Value="0,12,0,0"/>
    </Style>
    <Style Selector="Border.pt-lg">
        <Setter Property="Padding" Value="0,16,0,0"/>
    </Style>
    <Style Selector="Border.pb-xs">
        <Setter Property="Padding" Value="0,0,0,4"/>
    </Style>
    <Style Selector="Border.pb-sm">
        <Setter Property="Padding" Value="0,0,0,8"/>
    </Style>
    <Style Selector="Border.pb-md">
        <Setter Property="Padding" Value="0,0,0,12"/>
    </Style>
    <Style Selector="Border.pb-lg">
        <Setter Property="Padding" Value="0,0,0,16"/>
    </Style>
    <Style Selector="Border.pl-xs">
        <Setter Property="Padding" Value="4,0,0,0"/>
    </Style>
    <Style Selector="Border.pl-sm">
        <Setter Property="Padding" Value="8,0,0,0"/>
    </Style>
    <Style Selector="Border.pl-md">
        <Setter Property="Padding" Value="12,0,0,0"/>
    </Style>
    <Style Selector="Border.pl-lg">
        <Setter Property="Padding" Value="16,0,0,0"/>
    </Style>
    <Style Selector="Border.pr-xs">
        <Setter Property="Padding" Value="0,0,4,0"/>
    </Style>
    <Style Selector="Border.pr-sm">
        <Setter Property="Padding" Value="0,0,8,0"/>
    </Style>
    <Style Selector="Border.pr-md">
        <Setter Property="Padding" Value="0,0,12,0"/>
    </Style>
    <Style Selector="Border.pr-lg">
        <Setter Property="Padding" Value="0,0,16,0"/>
    </Style>

    <!-- Padding - Individual sides - TemplatedControl -->
    <Style Selector="TemplatedControl.pt-xs">
        <Setter Property="Padding" Value="0,4,0,0"/>
    </Style>
    <Style Selector="TemplatedControl.pt-sm">
        <Setter Property="Padding" Value="0,8,0,0"/>
    </Style>
    <Style Selector="TemplatedControl.pt-md">
        <Setter Property="Padding" Value="0,12,0,0"/>
    </Style>
    <Style Selector="TemplatedControl.pt-lg">
        <Setter Property="Padding" Value="0,16,0,0"/>
    </Style>
    <Style Selector="TemplatedControl.pb-xs">
        <Setter Property="Padding" Value="0,0,0,4"/>
    </Style>
    <Style Selector="TemplatedControl.pb-sm">
        <Setter Property="Padding" Value="0,0,0,8"/>
    </Style>
    <Style Selector="TemplatedControl.pb-md">
        <Setter Property="Padding" Value="0,0,0,12"/>
    </Style>
    <Style Selector="TemplatedControl.pb-lg">
        <Setter Property="Padding" Value="0,0,0,16"/>
    </Style>
    <Style Selector="TemplatedControl.pl-xs">
        <Setter Property="Padding" Value="4,0,0,0"/>
    </Style>
    <Style Selector="TemplatedControl.pl-sm">
        <Setter Property="Padding" Value="8,0,0,0"/>
    </Style>
    <Style Selector="TemplatedControl.pl-md">
        <Setter Property="Padding" Value="12,0,0,0"/>
    </Style>
    <Style Selector="TemplatedControl.pl-lg">
        <Setter Property="Padding" Value="16,0,0,0"/>
    </Style>
    <Style Selector="TemplatedControl.pr-xs">
        <Setter Property="Padding" Value="0,0,4,0"/>
    </Style>
    <Style Selector="TemplatedControl.pr-sm">
        <Setter Property="Padding" Value="0,0,8,0"/>
    </Style>
    <Style Selector="TemplatedControl.pr-md">
        <Setter Property="Padding" Value="0,0,12,0"/>
    </Style>
    <Style Selector="TemplatedControl.pr-lg">
        <Setter Property="Padding" Value="0,0,16,0"/>
    </Style>

    <!-- ============================================== -->
    <!-- GAP / SPACING UTILITIES (StackPanel)           -->
    <!-- ============================================== -->

    <Style Selector="StackPanel.gap-0">
        <Setter Property="Spacing" Value="0"/>
    </Style>
    <Style Selector="StackPanel.gap-xxs">
        <Setter Property="Spacing" Value="2"/>
    </Style>
    <Style Selector="StackPanel.gap-xs">
        <Setter Property="Spacing" Value="4"/>
    </Style>
    <Style Selector="StackPanel.gap-sm">
        <Setter Property="Spacing" Value="8"/>
    </Style>
    <Style Selector="StackPanel.gap-md">
        <Setter Property="Spacing" Value="12"/>
    </Style>
    <Style Selector="StackPanel.gap-lg">
        <Setter Property="Spacing" Value="16"/>
    </Style>
    <Style Selector="StackPanel.gap-xl">
        <Setter Property="Spacing" Value="24"/>
    </Style>
    <Style Selector="StackPanel.gap-2xl">
        <Setter Property="Spacing" Value="32"/>
    </Style>

    <!-- Semantic gap for icon+text pairs (6px) -->
    <Style Selector="StackPanel.gap-icon">
        <Setter Property="Spacing" Value="6"/>
    </Style>

    <!-- ============================================== -->
    <!-- SIZE UTILITIES                                 -->
    <!-- ============================================== -->

    <!-- Width -->
    <Style Selector="Control.w-full">
        <Setter Property="HorizontalAlignment" Value="Stretch"/>
    </Style>
    <Style Selector="Control.w-auto">
        <Setter Property="HorizontalAlignment" Value="Left"/>
    </Style>

    <!-- Height -->
    <Style Selector="Control.h-full">
        <Setter Property="VerticalAlignment" Value="Stretch"/>
    </Style>
    <Style Selector="Control.h-auto">
        <Setter Property="VerticalAlignment" Value="Top"/>
    </Style>

    <!-- ============================================== -->
    <!-- ALIGNMENT UTILITIES                            -->
    <!-- ============================================== -->

    <!-- Horizontal Alignment (items-*) -->
    <Style Selector="Control.items-start">
        <Setter Property="HorizontalAlignment" Value="Left"/>
    </Style>
    <Style Selector="Control.items-center">
        <Setter Property="HorizontalAlignment" Value="Center"/>
    </Style>
    <Style Selector="Control.items-end">
        <Setter Property="HorizontalAlignment" Value="Right"/>
    </Style>
    <Style Selector="Control.items-stretch">
        <Setter Property="HorizontalAlignment" Value="Stretch"/>
    </Style>

    <!-- Vertical Alignment (justify-*) -->
    <Style Selector="Control.justify-start">
        <Setter Property="VerticalAlignment" Value="Top"/>
    </Style>
    <Style Selector="Control.justify-center">
        <Setter Property="VerticalAlignment" Value="Center"/>
    </Style>
    <Style Selector="Control.justify-end">
        <Setter Property="VerticalAlignment" Value="Bottom"/>
    </Style>
    <Style Selector="Control.justify-stretch">
        <Setter Property="VerticalAlignment" Value="Stretch"/>
    </Style>

    <!-- Center both axes -->
    <Style Selector="Control.center">
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
    </Style>

    <!-- ============================================== -->
    <!-- LAYOUT UTILITIES                               -->
    <!-- ============================================== -->

    <!-- Flex direction (for StackPanel) -->
    <Style Selector="StackPanel.flex-row">
        <Setter Property="Orientation" Value="Horizontal"/>
    </Style>
    <Style Selector="StackPanel.flex-col">
        <Setter Property="Orientation" Value="Vertical"/>
    </Style>

    <!-- Cursor -->
    <Style Selector="Control.cursor-pointer">
        <Setter Property="Cursor" Value="Hand"/>
    </Style>
    <Style Selector="Control.cursor-default">
        <Setter Property="Cursor" Value="Arrow"/>
    </Style>

    <!-- ============================================== -->
    <!-- OPACITY UTILITIES                              -->
    <!-- ============================================== -->

    <Style Selector="Control.opacity-0">
        <Setter Property="Opacity" Value="0"/>
    </Style>
    <Style Selector="Control.opacity-50">
        <Setter Property="Opacity" Value="0.5"/>
    </Style>
    <Style Selector="Control.opacity-75">
        <Setter Property="Opacity" Value="0.75"/>
    </Style>
    <Style Selector="Control.opacity-100">
        <Setter Property="Opacity" Value="1"/>
    </Style>

    <!-- ============================================== -->
    <!-- BORDER RADIUS UTILITIES                        -->
    <!-- ============================================== -->

    <Style Selector="Border.rounded-none">
        <Setter Property="CornerRadius" Value="0"/>
    </Style>
    <Style Selector="Border.rounded-sm">
        <Setter Property="CornerRadius" Value="4"/>
    </Style>
    <Style Selector="Border.rounded-md">
        <Setter Property="CornerRadius" Value="8"/>
    </Style>
    <Style Selector="Border.rounded-lg">
        <Setter Property="CornerRadius" Value="12"/>
    </Style>
    <Style Selector="Border.rounded-xl">
        <Setter Property="CornerRadius" Value="16"/>
    </Style>
    <Style Selector="Border.rounded-2xl">
        <Setter Property="CornerRadius" Value="20"/>
    </Style>

    <!-- ============================================== -->
    <!-- BACKGROUND COLOR UTILITIES                     -->
    <!-- ============================================== -->

    <!-- Background - Border -->
    <Style Selector="Border.bg-transparent">
        <Setter Property="Background" Value="Transparent"/>
    </Style>
    <Style Selector="Border.bg-surface">
        <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
    </Style>
    <Style Selector="Border.bg-elevated">
        <Setter Property="Background" Value="{DynamicResource SurfaceElevatedBrush}"/>
    </Style>
    <Style Selector="Border.bg-card">
        <Setter Property="Background" Value="{DynamicResource CardBackgroundBrush}"/>
    </Style>
    <Style Selector="Border.bg-accent">
        <Setter Property="Background" Value="{DynamicResource AccentBrush}"/>
    </Style>
    <Style Selector="Border.bg-success">
        <Setter Property="Background" Value="{DynamicResource SuccessBrush}"/>
    </Style>
    <Style Selector="Border.bg-error">
        <Setter Property="Background" Value="{DynamicResource ErrorBrush}"/>
    </Style>

    <!-- Background - TemplatedControl -->
    <Style Selector="TemplatedControl.bg-transparent">
        <Setter Property="Background" Value="Transparent"/>
    </Style>
    <Style Selector="TemplatedControl.bg-surface">
        <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
    </Style>
    <Style Selector="TemplatedControl.bg-elevated">
        <Setter Property="Background" Value="{DynamicResource SurfaceElevatedBrush}"/>
    </Style>
    <Style Selector="TemplatedControl.bg-card">
        <Setter Property="Background" Value="{DynamicResource CardBackgroundBrush}"/>
    </Style>
    <Style Selector="TemplatedControl.bg-accent">
        <Setter Property="Background" Value="{DynamicResource AccentBrush}"/>
    </Style>
    <Style Selector="TemplatedControl.bg-success">
        <Setter Property="Background" Value="{DynamicResource SuccessBrush}"/>
    </Style>
    <Style Selector="TemplatedControl.bg-error">
        <Setter Property="Background" Value="{DynamicResource ErrorBrush}"/>
    </Style>

    <!-- ============================================== -->
    <!-- TEXT COLOR UTILITIES                           -->
    <!-- ============================================== -->

    <!-- TextBlock -->
    <Style Selector="TextBlock.text-primary">
        <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
    </Style>
    <Style Selector="TextBlock.text-secondary">
        <Setter Property="Foreground" Value="{DynamicResource TextSecondaryBrush}"/>
    </Style>
    <Style Selector="TextBlock.text-muted">
        <Setter Property="Foreground" Value="{DynamicResource TextMutedBrush}"/>
    </Style>
    <Style Selector="TextBlock.text-accent">
        <Setter Property="Foreground" Value="{DynamicResource AccentBrush}"/>
    </Style>
    <Style Selector="TextBlock.text-success">
        <Setter Property="Foreground" Value="{DynamicResource SuccessBrush}"/>
    </Style>
    <Style Selector="TextBlock.text-error">
        <Setter Property="Foreground" Value="{DynamicResource ErrorBrush}"/>
    </Style>
    <Style Selector="TextBlock.text-warning">
        <Setter Property="Foreground" Value="{DynamicResource WarningBrush}"/>
    </Style>

    <!-- SelectableTextBlock -->
    <Style Selector="SelectableTextBlock.text-primary">
        <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
    </Style>
    <Style Selector="SelectableTextBlock.text-secondary">
        <Setter Property="Foreground" Value="{DynamicResource TextSecondaryBrush}"/>
    </Style>
    <Style Selector="SelectableTextBlock.text-muted">
        <Setter Property="Foreground" Value="{DynamicResource TextMutedBrush}"/>
    </Style>
    <Style Selector="SelectableTextBlock.text-accent">
        <Setter Property="Foreground" Value="{DynamicResource AccentBrush}"/>
    </Style>
    <Style Selector="SelectableTextBlock.text-success">
        <Setter Property="Foreground" Value="{DynamicResource SuccessBrush}"/>
    </Style>
    <Style Selector="SelectableTextBlock.text-error">
        <Setter Property="Foreground" Value="{DynamicResource ErrorBrush}"/>
    </Style>
    <Style Selector="SelectableTextBlock.text-warning">
        <Setter Property="Foreground" Value="{DynamicResource WarningBrush}"/>
    </Style>

    <!-- MaterialIcon -->
    <Style Selector="mi|MaterialIcon.text-primary">
        <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
    </Style>
    <Style Selector="mi|MaterialIcon.text-secondary">
        <Setter Property="Foreground" Value="{DynamicResource TextSecondaryBrush}"/>
    </Style>
    <Style Selector="mi|MaterialIcon.text-muted">
        <Setter Property="Foreground" Value="{DynamicResource TextMutedBrush}"/>
    </Style>
    <Style Selector="mi|MaterialIcon.text-accent">
        <Setter Property="Foreground" Value="{DynamicResource AccentBrush}"/>
    </Style>
    <Style Selector="mi|MaterialIcon.text-success">
        <Setter Property="Foreground" Value="{DynamicResource SuccessBrush}"/>
    </Style>
    <Style Selector="mi|MaterialIcon.text-error">
        <Setter Property="Foreground" Value="{DynamicResource ErrorBrush}"/>
    </Style>
    <Style Selector="mi|MaterialIcon.text-warning">
        <Setter Property="Foreground" Value="{DynamicResource WarningBrush}"/>
    </Style>

    <!-- ============================================== -->
    <!-- DIVIDER / SEPARATOR UTILITIES                  -->
    <!-- ============================================== -->

    <!-- Vertical divider (for horizontal layouts) -->
    <Style Selector="Border.divider-v">
        <Setter Property="Width" Value="1"/>
        <Setter Property="Margin" Value="0,4"/>
        <Setter Property="Background" Value="{DynamicResource BorderSubtleBrush}"/>
    </Style>

    <!-- Horizontal divider (for vertical layouts) -->
    <Style Selector="Border.divider-h">
        <Setter Property="Height" Value="1"/>
        <Setter Property="Margin" Value="4,0"/>
        <Setter Property="Background" Value="{DynamicResource BorderSubtleBrush}"/>
    </Style>

    <!-- ============================================== -->
    <!-- VISIBILITY UTILITIES                           -->
    <!-- ============================================== -->

    <Style Selector="Control.hidden">
        <Setter Property="IsVisible" Value="False"/>
    </Style>
    <Style Selector="Control.visible">
        <Setter Property="IsVisible" Value="True"/>
    </Style>

    <!-- ============================================== -->
    <!-- TEXT ALIGNMENT UTILITIES                       -->
    <!-- ============================================== -->

    <Style Selector="TextBlock.text-left">
        <Setter Property="TextAlignment" Value="Left"/>
    </Style>
    <Style Selector="TextBlock.text-center">
        <Setter Property="TextAlignment" Value="Center"/>
    </Style>
    <Style Selector="TextBlock.text-right">
        <Setter Property="TextAlignment" Value="Right"/>
    </Style>

    <Style Selector="SelectableTextBlock.text-left">
        <Setter Property="TextAlignment" Value="Left"/>
    </Style>
    <Style Selector="SelectableTextBlock.text-center">
        <Setter Property="TextAlignment" Value="Center"/>
    </Style>
    <Style Selector="SelectableTextBlock.text-right">
        <Setter Property="TextAlignment" Value="Right"/>
    </Style>

    <!-- ============================================== -->
    <!-- TEXT OVERFLOW UTILITIES                        -->
    <!-- ============================================== -->

    <Style Selector="TextBlock.truncate">
        <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
    </Style>
    <Style Selector="TextBlock.wrap">
        <Setter Property="TextWrapping" Value="Wrap"/>
    </Style>
    <Style Selector="TextBlock.nowrap">
        <Setter Property="TextWrapping" Value="NoWrap"/>
    </Style>

    <Style Selector="SelectableTextBlock.truncate">
        <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
    </Style>
    <Style Selector="SelectableTextBlock.wrap">
        <Setter Property="TextWrapping" Value="Wrap"/>
    </Style>
    <Style Selector="SelectableTextBlock.nowrap">
        <Setter Property="TextWrapping" Value="NoWrap"/>
    </Style>

</Styles>
